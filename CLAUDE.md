# Project Identity

这个项目是一款 AI 辅助阅读工具。
核心原则不是“替用户读”，而是“陪用户读得更快、更深、更有留存”。

产品目标：
- 压缩低价值阅读时间
- 放大高价值段落理解
- 自动化读后沉淀
- 帮助用户形成自己的理解，而不是只消费 AI 结论

## Product Guardrails

永远不要把产品做成：
- 全书摘要工具
- 通用文档问答壳子
- “替你读完”的消费型工具
- 先给答案、后补来源的工具

永远优先支持：
- 可变粒度阅读
- 阅读过程中的实时辅助
- 与个人知识库的连接
- 反理解幻觉设计
- 所有 AI 结论可回看原文

## MVP Scope

Phase 0 只允许做以下能力：
1. 结构地图（章节 -> 核心论点）
2. 段落压缩
3. 段落展开
4. 选中文本即问
5. 自动生成结构化读书笔记

Phase 0 明确不做：
- 完整阅读器
- 推荐系统
- 社交功能
- 移动端
- 多人协作
- 公共书库
- 复杂知识图谱可视化

任何新需求都必须先判断：
- 它是否直接服务“陪你读”？
- 它是否属于 MVP 必需项？
- 不做它会不会影响核心价值闭环？

## Tech Direction

当前技术方向：
- 宿主形态：Obsidian 插件优先
- 插件：TypeScript + Obsidian Plugin API
- AI 层：独立 service 层封装，不把模型调用散落在 UI
- 输出：优先结构化 JSON，再映射到 UI
- 笔记沉淀：Markdown 优先，兼容 Obsidian 双链

## Architecture Rules

必须遵守：
- UI 层不直接拼 prompt
- 所有 prompt 放在 `prompts/` 目录
- 所有模型输出都要经过 schema 校验
- 所有 parser 逻辑独立到 `src/core/parser/`
- 所有 AI orchestration 独立到 `src/core/ai/`
- 所有 Obsidian 适配逻辑独立到 `src/adapters/obsidian/`
- 所有核心类型定义集中在 `src/types/`
- 所有命令注册集中管理，不在多个文件里散落注册

推荐目录：
- `src/core/parser/`
- `src/core/ai/`
- `src/core/note-generation/`
- `src/adapters/obsidian/`
- `src/ui/`
- `src/commands/`
- `src/types/`
- `prompts/`
- `tests/fixtures/`

## UX Rules

任何 AI 能力都必须满足：
- 能解释“为什么这样总结”
- 能回到原文段落
- 不制造“我已经理解了”的幻觉
- 不默认替用户下结论
- 优先给结构、对比、问题、关联，而不是直接代替思考

段落压缩：
- 目标是压缩低价值信息，不是把原文整体缩短
- 必须保留论点、转折、反例、限定条件
- 输出里要标明“这是一种阅读辅助，而不是原文替代”

段落展开：
- 必须帮助用户看清上下文、论证链、例子与隐藏前提
- 不允许无根据扩写

读后笔记：
- 必须包含“核心观点 / 值得记住的点 / 可连接旧知 / 待追问问题”
- 不生成空洞鸡汤式总结

## Development Workflow

默认工作流：
1. 先探索代码和需求，不急着写
2. 先给实施计划，再开始改
3. 改动前先列风险和回归点
4. 改动后必须给验证方式
5. 核心链路必须有测试或 fixture
6. 当上下文污染时，主动建议切换子任务或 subagent

## Testing Rules

必须优先补测试的模块：
- 文本切分
- 标题识别
- 段落边界判断
- 结构地图生成
- 压缩/展开输出 schema
- 笔记生成格式

改动核心逻辑时必须：
- 提供至少 1 个正常样例
- 提供至少 1 个边界样例
- 提供至少 1 个失败样例
- 说明本次改动最可能引入的回归

## Coding Style

- 先小步改，再扩
- 先修结构，再补功能
- 不为了“聪明”牺牲可维护性
- 不引入重量级依赖去解决小问题
- 引入新依赖时说明必要性、替代方案、潜在风险
- 不把业务规则硬编码进 UI 文案

## Review Checklist

每次完成任务后都要自查：
- 是否偏离了 MVP
- 是否把“陪你读”做成了“替你读”
- 是否保留原文可追溯性
- 是否增加了不必要复杂度
- 是否给出了验证方法